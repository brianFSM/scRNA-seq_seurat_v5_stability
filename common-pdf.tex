% pure LaTeX header (no YAML here)
\usepackage[hyphens]{url}
\usepackage{hyperref}
\usepackage{bookmark}
\usepackage{pdflscape}
\newcommand{\blandscape}{\begin{landscape}}
\newcommand{\elandscape}{\end{landscape}}

% allow nice URL/path breaks
\Urlmuskip=0mu plus 1mu
\def\UrlBreaks{\do\/\do-\do\_\do.}

% make the PDF open with bookmarks and colored links
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue,
  pdfpagemode=UseOutlines,
  pdfstartview=FitH
}
\bookmarksetup{open,numbered,depth=3}

% (optional) relax overfull boxes a bit
\setlength{\emergencystretch}{3em}
